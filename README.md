## v0.14.3 修复版
- 修复「NPC互聊」按钮：生成结束后正确恢复按钮状态与生成锁，不再永远卡在“生成中…”，也不再死锁其他生成功能。
- 修复得文商店 52Poké 同步：
  - 补全「贵重道具 / 化石 / 食材 / Ｚ纯晶（宝可梦 / 训练家）」等百科分类映射，h2 标题同样参与匹配，同步道具量由约 10 件提升至 160+ 件。
  - 同步阈值改为容错判定（解析到 10 件以上即采用），内置兜底道具中未被百科覆盖的条目会合并保留。
  - 同步失败后 5 分钟内不再自动重复请求；有效缓存阈值由 100 件降为 40 件。
- 得文商店所有渲染文本统一 HTML 转义，外链仅允许 52Poké 站内地址。
- 夜间模式补全：微信式聊天、联系人设置、帖子评论区、世界书、得文商店等界面不再出现大片纯白。
- 修复洛托姆按钮 SVG 渐变的非法 offset 值，控制台不再报错。
- 统一版本号为 0.14.3（此前 manifest / 注释 / 代码内部三处不一致）；移除重复的悬浮按钮位置恢复调用。

## 0.14.2 得文全道具版
- 得文商店自动同步 52Poké《道具列表》全部正式道具。
- 按百科页面主分类整理，支持搜索、分类、分页加载。
- 保留已有朱紫购买价；百科没有直接列出购买价的道具显示“价格待核实”，不会伪造价格。
- 可手动点击“同步百科”刷新数据。


## v0.14.1
- 新增「得文商店」手机 App：网购式首页、分类、搜索、商品详情、购物车、订单与余额。
- 商店道具分类参考神奇宝贝百科道具列表；默认价格采用《宝可梦 朱／紫》购买价。

# v0.14.0 修复版

- 洛托姆悬浮窗：单击打开，再单击关闭。
- 去除手机右上角独立 X。
- 缩放键移入状态栏右侧。
- 强化 Android WebView 触摸拖动。

# 宝可梦论坛纯白怪力（SillyTavern 扩展版 v0.41）

基于酒馆助手脚本 **测试论坛 0.331** 完整转换，**不依赖 Tavern Helper**。

## 安装

1. 将本文件夹放到：
   - `data/<用户名>/extensions/pkmn-phone-forum/`
   - 或 `public/scripts/extensions/third-party/pkmn-phone-forum/`
2. 确认目录内直接有 `manifest.json`、`index.js`、`style.css`
3. 刷新 SillyTavern，在扩展面板启用「宝可梦论坛纯白怪力」
4. 右下角出现洛托姆手机按钮

## 功能

- 安全 / 成熟双论坛、发帖回复、NPC 互聊
- 正文读取 + 世界书逐条勾选；勾选条目统一提供给所有 AI 模块
- 独立 API 配置
- 按聊天独立存档（chatId + localStorage + chatMetadata）
- 帖子注入正文（setExtensionPrompt）

## 原生 API 映射

| 原酒馆助手 | 扩展实现 |
|-----------|---------|
| getChatMessages | context.chat |
| getWorldbookNames / getWorldbook | getWorldInfoNames / loadWorldInfo + API 兜底 |
| eventOn | eventSource.on |
| injectPrompts | setExtensionPrompt |
| updateChatMetadata / saveChat | context 同名 API |

## 版本

- 0.41.0：基于 0.331 干净重建，含世界书与切聊天存档修复


## v0.50 修复
- 修复手机窗口初始定位：JS 使用 left/top 视口坐标时不再叠加 translate(-50%, -50%)。
- 恢复并固定洛托姆悬浮按钮的 fixed 定位、尺寸、层级与触摸拖动区域。
- 版本号更新为 0.50.0。


v0.55：彻底移除上一版洛托姆悬浮按钮拖动实现，重写为独立 drag session；Pointer Events + Android Touch Events 双路径，document/window 双兜底；超过6px才判定拖动，单击仍负责打开/关闭。


## v0.58 修复
- 通讯录设置新增「读取论坛全部内容」开关，默认开启并持久化。
- 「此聊天与论坛联动」改为联系人独立持久化，开关变更立即保存，重新进入联系人设置后保持状态。
- 新建联系人默认开启论坛联动。
- 升级迁移逻辑不再把用户主动关闭的论坛联动开关重新改回开启。

## v0.14.0 世界书重构
- 世界书设置改为逐本展开、Entry 独立勾选。
- 支持搜索、已选/未选/启用/停用筛选、Entry 详情与完整内容查看。
- 已勾选 Entry 强制注入所有 AI 模块，不再使用关键词智能筛选或 36 条/26k 字符硬上限。
- 世界书采用运行时缓存与按需读取，面向 100,000+ TK 大型世界书优化。
- 世界书读取失败不再静默吞掉；刷新可清空缓存并重新读取。
- 保留旧版仅选择“整本世界书”的配置，并在首次读取时迁移为逐条选择。
